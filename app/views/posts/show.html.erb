<h2><%= @post.title %></h2>
<p><%= @post.body %></p>

<!---------- ADMINS ONLY ---------->
<%= link_to "Edit", edit_post_path(@post) %>
<!---------- ADMINS ONLY ---------->

<% @post.comments.each do |pc| %>
	<h4><%= pc.author %></h4>
	<p><%= pc.comment_body %></p>
	

	<!---------- Need to pass the parent object and child variable from the loop ---------->	
	<%= link_to "Show comment", post_comment_path(@post, pc) %>
	<!---------- Need to pass the parent object and child variable from the loop ---------->	
<% end %>

<%= link_to "Add Comment", new_post_comment_path(@post) %>